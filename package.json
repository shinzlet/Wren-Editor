{
  "name": "Wren",
  "version": "0.0.0",
  "description": "GUI interface for a lightweight TI-Basic bichrome sprite renderer.",
  "main": "app.js",
  "scripts": {
    "start": "electron app.js",
    "watch-css": "node-sass -w sass/ -o css",
    "build-osx": "electron-packager . --overwrite --platform=darwin --arch=x64 --electron-version=1.5.0 --prune=true --out=bin"
  },
  "author": "Seth Hinz",
  "license": "MIT",
  "devDependencies": {
    "electron": "^1.4.15",
    "electron-packager": "^8.5.1",
    "node-sass": "^4.3.0"
  },
  "dependencies": {
    "jquery": "^1.7.4"
  }
}
